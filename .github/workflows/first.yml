name: CI


on:
  
  push:
    branches: [ "master" ]
  pull_request: 
    branches: ["master"]

jobs:
  build: 
    runs-on: ubuntu-latest 
    strategy: 
      matrix: 
        os: [ubuntu-latest,macos-latest]
        node: [14,15]
    steps: 
      - name: checkout the code 
        uses: actions/checkout@v2 
      - name: install dependecies 
        run: npm install 
      - name: build 
        run: echo "build done here" 
        
  

